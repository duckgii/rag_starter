# 14 CFR 에이전트형 RAG — 발표 스크립트

> 미국 항공 규정(14 CFR)에 대한 질문에, **원문 출처까지 인용**하며 답하는
> 구조 인식형(structure-aware) 에이전트 검색 시스템입니다.
> 흐름: **먼저 구조 설명**(툴 → 프롬프트 → 인덱싱), 그다음 **우리가 겪은 문제,
> 검토한 방안들, 장단점 비교, 우리가 선택한 방법.**

---

## 0. 한 줄 소개

> "미국 연방 항공 규정에 대한 Q&A 어시스턴트를 만들었습니다. 흔한 방식인 'PDF를
> 잘게 잘라 임베딩하고 제일 비슷한 조각이 걸리길 기대하는' 대신, 우리 에이전트는
> *사람이 규정을 읽듯이* — 목차를 검색하고, 섹션을 통째로 읽고, 상호참조를
> 따라가며 — 그리고 **자신이 읽은 것만 정확히 인용**합니다."

---

## PART 1 — 시스템 구조

### 슬라이드 1 · 문제와 코퍼스

- **도메인:** 14 CFR (연방규정집 Title 14 — 항공·우주).
- **코퍼스:** `documents/`의 공식 PDF 6개 — Part 61(조종사 자격), Part 67(신체검사
  기준), Part 71(공역 지정), Part 73(특수용도 공역), Part 91(운항 규칙), Volume 1.
- **왜 어려운가:** 실제 질문의 답은 보통 (a) 한 섹션 안에 흩어져 있고, (b) 그
  섹션이 **참조하는 다른 섹션**에 의존합니다. 단순 청크 검색으론 답이 묻힙니다.

**말할 내용:** "규정은 산문이 아니라 **번호가 매겨진 계층 구조**입니다. 그 구조가
이 프로젝트의 핵심입니다."

---

### 슬라이드 2 · 전체 아키텍처

```
 documents/*.pdf
      │
      ▼
 indexer.py ──► sections.pkl   (CFR 섹션 전체 + 임베딩)
      │        └► index.pkl     (고정 크기 청크 — 베이스라인)
      ▼
 agent.py  ──► ReAct 루프: 라우팅 → navigate_toc → read_section → follow_refs → 답변
      │
      ▼
 backend/app.py (Flask)  ──► 이벤트를 Server-Sent Events로 스트리밍
      │
      ▼
 frontend (React)  ──► 스트리밍 답변, 에이전트 궤적, 비용 패널, 링크된 인용
```

4개 계층: **인덱싱 → 에이전트 → 백엔드 → 프론트엔드.** 하나씩 봅니다.

---

### 슬라이드 3 · 인덱싱 — 두 가지 전략 (`indexer.py`)

같은 PDF에서 **두 개**의 인덱스를 만듭니다:

1. **청크 인덱스 (`index.pkl`) — 베이스라인.**
   `chunk_text()`가 텍스트를 ~1000자 겹침 청크(200자 오버랩)로 분할, 문단 경계
   우선. 전형적인 RAG 방식.

2. **섹션 인덱스 (`sections.pkl`) — 에이전트가 사용.**
   `build_sections()`가 정규식으로 실제 섹션 헤더(`§ 61.109 Aeronautical
   experience.`)를 찾아 PDF를 **섹션 단위**로 쪼개고
   `{section_id, part, source, title, text, embedding}`를 저장.
   - 랭킹용 임베딩은 **`제목 + 본문 앞 500자`** — 제목과, "누구/무엇에 적용되는지"를
     밝히는 첫 문장들.
   - `find_section_refs()`가 각 섹션이 인용하는 섹션들을 추출 → `follow_refs`의 근거.

**임베딩:** `sentence-transformers` 다국어 MiniLM, 384차원, 단위 정규화,
**로컬 실행(API 키 불필요)**. 코사인 거리 = `1 − 내적`.

**말할 내용:** "청킹은 문서의 구조를 버립니다. 섹션 인덱싱은 구조를 **보존**하고,
그 구조를 나중에 그대로 활용합니다."

---

### 슬라이드 4 · 툴 (`agent.py`)

에이전트에겐 3개의 툴 — 사람이 규정을 읽을 때 쓰는 동사들:

| 툴 | 하는 일 | 반환 |
|---|---|---|
| `navigate_toc(query)` | 섹션 인덱스 의미 검색 | 상위 후보: id, 제목, 120자 프리뷰 |
| `read_section(id)` | 섹션 하나를 전문 읽기 | 전문(최대 18k자) + 상호참조 목록 |
| `follow_refs(id)` | 그 섹션이 인용한 섹션들 나열 | 참조 섹션 프리뷰 |

**ReAct 루프**: 추론 → 툴 호출 → 결과 관찰 → 반복, 답할 수 있을 때까지. 모든
주장은 실제로 읽은 섹션에 매핑되는 `[n]`으로 인용됩니다.

**말할 내용:** "`navigate_toc`는 *어디를* 볼지 찾고, `read_section`은 읽고,
`follow_refs`는 인용 그래프를 걷습니다. 3개의 기본기 = 사람의 작업 흐름."

---

### 슬라이드 5 · 프롬프트 & Adaptive-RAG 라우팅

- **시스템 프롬프트**: 툴로 읽은 텍스트만 사용, 프리뷰에서 올바른 섹션 선택(제목이
  겹침 — 학생/자가용/사업용/ATP), `[n]` 인용, 답변 품질 규칙 준수(뒤에서 설명).

- **Adaptive-RAG 라우터:** 저렴한 **Haiku** 호출이 질문 복잡도를 분류해 한 글자만
  반환:

  | 티어 | 의미 | 예산 |
  |---|---|---|
  | **A** | 검색 불필요(인사/범위 밖) | 0회 반복, 즉답 |
  | **B** | 단일 섹션 사실 | 2회 반복, `follow_refs` 없음 |
  | **C** | 다중 섹션/상호참조 | 6회 반복, `follow_refs` 사용 |

**말할 내용:** "'안녕'에 6단계 에이전트를 돌리지 않습니다. 라우터가 질문에 걸맞은
예산만 씁니다."

---

### 슬라이드 6 · 비용 엔지니어링 — 프롬프트 캐싱 & 스트리밍

- **프롬프트 캐싱:** ReAct 매 반복은 커진 히스토리 전체(읽은 섹션 전부)를 다시
  보냅니다. `_cache_last()`가 캐시 경계를 최신 메시지로 옮겨, 반복되는 컨텍스트를
  **저렴한 캐시 read(0.1배)**로 청구합니다. write는 1.25배, read는 0.1배 —
  **첫 재사용부터 바로 이득**.
- **스트리밍:** 백엔드(Flask)가 에이전트 이벤트를 **Server-Sent Events**로 전달 —
  `status`(실행 중인 툴), `delta`(답변 텍스트), `done`(인용 + 사용량). React UI가
  실시간 궤적, 토큰/비용 패널, 링크된 출처를 렌더링.

**말할 내용:** "UI의 비용 패널은 실제 값입니다 — 답변별 input / cache read /
cache write를 드러내서 트레이드오프를 숨기지 않았습니다."

---

### 슬라이드 7 · UI — 유리상자(glass-box) 에이전트 (`frontend/src/App.jsx`)

에이전트의 작업을 **투명하고 검증 가능하게** — 블랙박스가 아니라 — 만드는 데
공을 들였습니다. 신경 쓴 부분:

- **실시간 스트리밍 답변.** 토큰이 도착하는 대로 렌더링(SSE `delta`) — 텍스트
  덩어리를 기다리지 않고 즉시 진행 상황을 봄.
- **에이전트 궤적 타임라인.** 접을 수 있는 단계 목록이 전체 실행을 보여줌: 라우팅
  결정(→ 티어), 각 툴 호출의 인자·지연시간·결과 토큰 수, 그리고 최종 답변. 에이전트가
  *어떻게* 결론에 도달했는지 보임.
- **토큰/비용 패널.** 답변별로 input vs output 토큰, billed input / cache read /
  cache write 누적 막대, 달러 비용, 캐시 절감률, 총 컨텍스트 크기 — 경제성을 화면에
  드러냄(숨기지 않음).
- **검증 가능한 인용.** 인라인 `[n]` 마커 클릭 시 해당 출처로 스크롤·펼침·**플래시
  하이라이트**. 각 출처는 읽기 쉬운 문서 제목과 eCFR + govinfo 소스 PDF 링크 제공.
- **실시간 상태 배너.** 애니메이션 점이 현재 단계 표시("Reading § 61.109…",
  "Following cross-references…").
- **프리앰블 숨김.** 스트리밍 턴이 툴 호출로 끝나면 그 텍스트는 모델의 사고 과정일
  뿐 — `reset_answer`로 버려 실제 답변만 남김.
- **상시 예시 질문.** 고정(sticky) 우측 사이드바(1–5 번호, 실행 중 비활성화, 좁은
  화면에선 스택 레이아웃)에서 언제든 샘플 질문 실행.
- **Stop 버튼**으로 스트리밍 중단; **Copy answer**로 출처 포함 전체 Markdown 내보내기.

**말할 내용:** "에이전트에 대한 신뢰는 투명성에서 옵니다. 모든 답변이 *무엇을 했고,
얼마가 들었고, 어디서 왔는지*를 보여줍니다 — 궤적, 비용 패널, 클릭 가능한 인용은
모두 하나의 아이디어: 블랙박스 없음."

---

## PART 2 — 문제, 검토한 방안, 우리의 선택

> 각 문제마다: 무엇이 깨졌는지, 비교한 방안과 장단점, 그리고 **실측에 근거한** 결정.

### 슬라이드 8 · 문제 1 — 청크 검색이 답을 묻는다

- **증상:** 고정 크기 청크에선 정답 섹션이 거의 동일한 청크들 사이에서 ~17위로
  밀리고, 내용도 여러 청크로 쪼개짐.
- **방안:**
  - *(a)* 청크 크게 / k 늘리기 → 구조는 여전히 없음, 노이즈만 증가.
  - *(b)* **섹션 단위 인덱스 + 에이전트 내비게이션.** ✅
- **트레이드오프:** 에이전트는 단발 검색보다 질의당 비용이 크지만, **완전한** 규정을
  읽고 참조를 따라갈 수 있음 — 청킹은 근본적으로 불가능.
- **선택:** *(b)* — 핵심 아키텍처(커밋 `0020ae7`).

---

### 슬라이드 9 · 문제 2 — 획일적 예산은 낭비다

- **증상:** 모든 메시지(인사조차)에 풀 에이전트 루프를 돌리면 토큰·지연 낭비.
- **방안:**
  - *(a)* 항상 풀 루프 → 단순하지만 비쌈.
  - *(b)* **Adaptive-RAG 티어 라우팅(A/B/C).** ✅
- **트레이드오프:** 라우팅은 저렴한 Haiku 1회 + 오분류 위험 — Tier B의 2회 예산이
  빠듯함(navigate → read → answer는 3턴 필요). 문제 3의 안전망으로 완화.
- **선택:** *(b)* — 복잡도에 맞춘 예산.

---

### 슬라이드 10 · 문제 3 — 집계형 질문이 실패한다  ⭐ 핵심 스토리

**사례 — Q2:** *"1급 항공 신체검사 증명서의 결격 사유가 되는 의학적 조건은?"*

- 답이 **6개 형제 섹션**에 흩어짐 (§67.103 눈, .105 귀, .107 정신, .109 신경,
  .111 심혈관, .113 일반).
- `navigate_toc` 상위 6개는 그중 **2~3개만** 노출 — 2급·3급 블록(§67.2xx /
  §67.3xx, **제목이 동일!**)의 디코이에 밀림.
- 에이전트는 3번 재검색하다 6회 예산을 소진하고 **답 없이 포기** — ~$0.036를 쓰고
  결과물 0.

**비교한 세 가지 방안:**

| 방안 | 아이디어 | 장점 | 단점 |
|---|---|---|---|
| **1. 강제 종합** | 예산 소진 시 포기 대신 읽은 것으로 답변 | 저렴; "무응답" 제거 | 커버리지 개선 없음 — §67.113 여전히 누락 |
| **2. Recall 확장 (핵심 채택)** | `navigate_toc`가 지배적 번호 블록(§67.1xx)을 통째로 가져오고 다른 블록 디코이 제거 | 커버리지 **와** 정밀도 동시 해결; 선형 비용 | 번호 구조 인식 + 크기 상한 필요 |
| **3. 반복 배증(iterative deepening)** | 실패 시 예산을 2→4→8→16으로 배증 재시도 | 구현 단순 | 지수적 비용; 근본 원인(낮은 recall) 미해결 — 더 오래 헤맬 뿐 |

**방안 2의 핵심 통찰:** CFR 번호가 구조를 **인코딩**함 — §67.**1**xx = 1급,
§67.**2**xx = 2급, §67.**3**xx = 3급. 그래서 히트가 한 작은 블록에 몰리면 **그
블록 전체**를 가져오고 다른 급수는 배제. 작은 블록(`≤ 14`개)에만 적용해 큰
서브파트(Part 91의 ~48개 §91.1xx)는 절대 쓸어담지 않음 — 나머지 4개 질문에
**회귀 없음** 검증 완료.

**우리의 선택:** **방안 2를 해결책으로 + 방안 1을 안전망으로.** 반복 배증은
기각 — 검색이 *왜* 놓쳤는지는 안 고치고 비용만 키우기 때문.

**실측 결과 (Q2, 라이브):**

| | 이전 | 강제 종합만 | **방안 2 (최종)** |
|---|---|---|---|
| 결과 | 포기, 답 없음 | 불완전(5/6, 누락 명시) | **완전(6/6)** |
| 인용 섹션 | 0 | 5 | **6** |
| 비용 | ~$0.036 | ~$0.080 | **~$0.057** |

→ 불완전한 답보다 **더 싼 값**에 **완전한** 답 — 확장이 에이전트의 헤맴을
없애기 때문.

---

### 슬라이드 11 · 문제 4 — 인용이 읽기 어렵고 검증 불가

- **증상:** PDF 텍스트에 강제 줄바꿈, 줄 끝 하이픈 분절(`un-\nless`), 페이지 푸터
  노이즈; UI는 원본 파일명만 보여줘 출처 확인 불가.
- **해결 (채택):**
  - 텍스트 정리(하이픈 결합, 푸터 제거, 줄바꿈 병합) 후 `(a)/(b)/(1)` 마커에서
    문단 분리.
  - **사람이 읽는 문서 제목** 표시("14 CFR Part 67 — Medical Standards and
    Certification").
  - 링크 2개: **eCFR**(해당 섹션으로 바로 점프) + **govinfo 소스 PDF**(우리가
    인덱싱한 바로 그 문서 — 파일명이 govinfo granule과 1:1, 둘 다 URL 유효성 확인).
  - "Copy answer"가 답변 + 출처 블록쿼트 전체를 Markdown으로 내보냄.

---

### 슬라이드 12 · 문제 5 — 답변 품질(전문용어, 구조, 장황함)

- **증상:** 약어 미설명, 한 줄 답변에 억지 헤딩, 질문 재진술.
- **해결 (채택):** 명시적 프롬프트 규칙 —
  - **약어는 첫 등장 시 한 번, 약어 우선:** `14 CFR (Code of Federal
    Regulations)`, `FFS (full flight simulator)` — *절대 역순 금지*; 이후 약어만.
  - **질문에 맞는 구조:** 단일 사실은 한 문장, 열거형은 리스트, **비교일 때만 표.**
  - **군더더기 금지:** 직답 먼저, 관련 없는 부분 제거.
- **흥미로운 발견:** *"including citation ones like CFR"* 문구가 결정적 —
  이게 없으면 모델이 "14 CFR"을 단순 인용으로 취급해 확장을 건너뜀. 라이브 실행으로
  검증한 뒤, 이 문구만 남기고 프롬프트를 압축.

**말할 내용:** "여기서 프롬프트 엔지니어링은 경험적이었습니다 — 한 줄 바꾸고,
질문을 실제로 돌려, 무슨 일이 실제로 벌어지는지 관찰했습니다."

---

### 슬라이드 13 · 결과 & 교훈

- **계층형 문서엔 청킹보다 구조가 낫다** — 문서 고유 단위로 인덱싱하고 내비게이션.
- **질문에 노력을 맞춰라**(Adaptive-RAG) — 비용 통제.
- **증상이 아니라 근본 원인을 고쳐라:** 집계형 실패에서, recall 확장(*왜* 놓쳤나를
  해결)이 반복 배증(그냥 더 쓰기)을 이겼다. 그 위에 저렴한 안전망을 얹었다.
- **출처를 일급으로:** 정리·문단화·**링크**된 인용이 데모를 신뢰할 수 있는 것으로.
- **투명성은 기능이다:** 궤적, 실시간 비용 패널, 클릭 가능한 인용이 에이전트를
  유리상자로 만든다 — 모든 답변을 보고 검증할 수 있다.
- **프롬프트 규칙은 추측이 아니라 라이브로 검증.**

**마무리 멘트:** "관통하는 주제 하나: *데이터의 구조를 활용하라.* 인덱싱, 툴,
검색 개선, 심지어 인용 링크까지 전부 이 원칙이 이끌었습니다."

---

### 부록 · 핵심 파일 & 파라미터

- `indexer.py` — `chunk_text`, `build_sections`, `split_sections`,
  `find_section_refs`, `embed`(로컬 MiniLM, 384차원).
- `agent.py` — 툴, `TIER_BUDGET`(A/B/C), `navigate_toc` + 블록 확장
  (`TOC_EXPAND_GROUP_MAX = 14`), 강제 종합 폴백, `_cache_last`.
- `backend/app.py` — Flask SSE 엔드포인트 `/api/chat`.
- `frontend/src/App.jsx` — 스트리밍 UI, 궤적, 비용 패널, 링크된 출처.
- 모델: 답변 = `claude-sonnet-4-6`, 라우터 = `claude-haiku-4-5`.
- 상수: `MAX_ITERS = 6`, `MAX_SECTION_CHARS = 18000`, `navigate_toc k = 6`.
